/**
 * Placeholder module.
 *
 * Your app uses an external realtime server (see lib/realtime-emit.ts),
 * so this file must be valid TypeScript to avoid build failures.
 *
 * If you later add an in-repo Socket.io server, implement it here.
 */
export {};
